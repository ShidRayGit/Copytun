# copyTun

`copyTun` یک اسکریپت **Bash منودار** برای ساخت و مدیریت تونل‌های **Half-Duplex** با استفاده از **WaterWall v1.41** است.

این اسکریپت به شما اجازه می‌دهد:
- روی یک سرور چندین تونل Half-Duplex داشته باشید
- هر تونل به‌صورت **سرویس systemd** اجرا شود
- تونل‌ها بعد از logout یا reboot قطع نشوند
- تونل‌ها را به‌راحتی **ایجاد، ویرایش، بررسی وضعیت یا حذف** کنید

---

## ✨ Features

- Menu-driven (منوی تعاملی)
- پشتیبانی از چند تونل همزمان (با نام‌های جدا)
- نصب خودکار WaterWall v1.41
- انتخاب باینری مناسب بر اساس:
  - amd64 / arm64
  - CPU قدیمی (old-cpu)
  - gcc / clang / avx512
- اجرای پایدار با systemd
- مدیریت کامل تونل‌ها:
  - Start / Restart
  - Stop
  - Status + Logs
  - Edit تنظیمات
  - Delete

---

## 🧱 Requirements

- Linux با **systemd**
- اجرای اسکریپت با **root** (`sudo`)
- یکی از ابزارهای دانلود:
  - `curl` یا `wget`
- یکی از ابزارهای unzip:
  - `unzip` یا `python3`

---

## 🚀 Installation & Run

ریپو را کلون کنید:

```bash
git clone https://github.com/ShidRayGit/copyTun.git
cd copyTun