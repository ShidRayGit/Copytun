# Copytun

**Copytun** یک اسکریپت **Bash منودار** برای ساخت و مدیریت تونل‌های **Half-Duplex** با استفاده از **WaterWall v1.41** است.

این اسکریپت به شما اجازه می‌دهد:
- چندین تونل Half-Duplex روی یک سرور داشته باشید
- هر تونل به‌صورت **سرویس systemd** اجرا شود
- تونل‌ها بعد از logout یا reboot قطع نشوند
- تونل‌ها را به‌راحتی **ایجاد، ویرایش، بررسی وضعیت یا حذف** کنید

---

## ✨ Features

- منوی تعاملی (Menu-Driven)
- پشتیبانی از چند تونل همزمان (Multi-Tunnel)
- نصب خودکار WaterWall v1.41
- انتخاب باینری مناسب بر اساس:
  - amd64 / arm64
  - CPU قدیمی (old-cpu)
  - gcc / clang / avx512
- اجرای پایدار با systemd
- مدیریت کامل تونل‌ها:
  - Start / Restart
  - Stop
  - Status + Logs
  - Edit تنظیمات
  - Delete

---

## 🧱 Requirements

- Linux با **systemd**
- اجرای اسکریپت با دسترسی **root**
- یکی از ابزارهای دانلود:
  - `wget` یا `curl`
- یکی از ابزارهای unzip:
  - `unzip` یا `python3`

---

## 🚀 Quick Run (One-Liner)

### با wget
```bash
sudo bash -c "$(wget -qO- https://raw.githubusercontent.com/ShidRayGit/Copytun/main/copytun.sh)"